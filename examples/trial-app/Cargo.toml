[package]
name = "trial-app"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
samd5-boot = { path = "../..", features = ["samd51j20a"] }
cortex-m = { version = "0.7.9", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7"
rtt-target = "0.5"
